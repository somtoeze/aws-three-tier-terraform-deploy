server:
  extraArgs: []  # Remove --insecure entirely
  # Optional: Add TLS configuration
  certificate:
    domain: argocd.nediva.online
    #issuer: letsencrypt-prod  # If using cert-manager

# password generation on argocd
# kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath='{.data.password}' | base64 -d
